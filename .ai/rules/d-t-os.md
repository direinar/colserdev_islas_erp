---
paths:
  - 'app/DTOs/**'
---

# D T Os

## Use lightweight DTO classes
Use simple DTO classes that hold array-backed data and expose get()/set() accessors. Keep DTOs lightweight and explicit instead of full data packages.
