---
paths:
  - 'app/Concerns/**'
---

# Concerns

## Validation helpers live in concerns traits
Place reusable validation rules in traits under app/Concerns/. Reuse shared validation methods across controllers, actions, and feature flows instead of duplicating rules inline.
